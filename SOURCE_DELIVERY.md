# Keystone source delivery

This archive is source-only. Generated build output, VSIX/package artifacts, local `.keystone` runtime state, `node_modules`, screenshots, logs, test suites, and verification harnesses are intentionally excluded.

Use `npm install --offline --ignore-scripts`, then `npm run build`, or open the folder in VS Code and press **F5** using the included launch configuration.
